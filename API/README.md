# API 

## Setup

Create database and put needed parameters into the `.env` file:

```
DB_HOST=
DB_PORT=
DB_USER=
DB_PASSWORD=
DB_DATABASE=
```

To run the project run this command:

```
npm run dev
```
